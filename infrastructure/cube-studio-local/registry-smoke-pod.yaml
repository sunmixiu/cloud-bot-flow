apiVersion: v1
kind: Pod
metadata:
  name: registry-smoke
  namespace: pipeline
spec:
  restartPolicy: Never
  containers:
    - name: smoke
      image: localhost:5001/cube-smoke:1.0
      imagePullPolicy: Always
      command: ["sh", "-c", "echo registry-pull-ok"]
